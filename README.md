# workout-plans
interview task

To start

 - clone the project `git clone https://github.com/LakyLak/workout-plans.git`
 - copy .env.example to .env
 - create db and set it up in .env
 - composer update
 - run `php db/migrations.php` for initial db setup
 
 

